# 🎨 Чистая Система Стилей на Дизайн-Токенах

Эта система позволяет создавать полностью унифицированные компоненты, которые автоматически обновляются при изменении дизайн-токенов.

## 🚀 Как использовать

### 1. Изменение цветов
```javascript
// Измените токены в brand-tokens/brand-colors.tokens.json
{
  "color": {
    "core": {
      "luxury": {
        "primary": {
          "600": { "$value": "#новый-цвет" }
        }
      }
    }
  }
}
```

### 2. Перегенерация CSS
```bash
node index.js transform brand-tokens.config.json
```

### 3. Перезагрузка в браузере
Нажмите "🔄 Перезагрузить токены" в демо или обновите страницу.

## 🎯 Преимущества

- ✅ **Полная унификация** - все компоненты используют одни токены
- ✅ **Мгновенные изменения** - изменения цветов применяются ко всем элементам
- ✅ **Масштабируемость** - легко добавлять новые компоненты
- ✅ **Консистентность** - гарантированная визуальная консистентность
- ✅ **Производительность** - CSS переменные для быстрого обновления

## 📋 Структура токенов

```
brand-tokens/
├── brand-colors.tokens.json      # Цветовая система
├── typography.tokens.json        # Типографика
├── components.tokens.json        # Компонентные стили
├── animation.tokens.json         # Анимации
└── responsive.tokens.json        # Адаптивность

ui-kit/
├── css/style.css                 # Чистые стили на токенах
├── js/app.js                     # Интерактивная логика
└── index.html                    # Демо интерфейс
```

## 🔧 Добавление новых компонентов

1. **Создайте HTML структуру**
2. **Добавьте стили в CSS, используя токены**
3. **Добавьте логику в JavaScript если нужно**
4. **Компонент автоматически унаследует все изменения токенов**

Пример:
```css
.my-component {
  background: var(--base-primary-600);
  border-radius: var(--radius-md);
  font-family: var(--typography-font-family-body);
}
```

## 🎨 Темы

Система поддерживает несколько встроенных тем:

- **Luxury** - Роскошная тема (по умолчанию)
- **Light** - Светлая тема
- **Dark** - Темная тема

Переключение тем происходит мгновенно и затрагивает все компоненты.

## ⚡ Производительность

- CSS переменные обновляются мгновенно
- Нет необходимости в перекомпиляции
- Минимальный JavaScript для динамических изменений
- Оптимизированные селекторы

---

**Результат:** Полностью унифицированная дизайн-система, где изменение одного токена мгновенно обновляет все компоненты! 🎨✨</contents>
</xai:function_call">Write contents to ui-kit/README.md.

When you're done with your current set of changes to this file, you should call the read_lints tool with the static file path and contents.